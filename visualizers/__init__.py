"""fiedler visualizers — presentation tooling (kept outside the src/ library).

  palette        Cityscapes class colours + label colorization
  seg_panels     [Raw | GT | Pred] triptych, overlay, per-class montage
  hyperspectral  datacube slicing: band-RGB, montage, PCA-RGB, signatures
  spectral_viz   Laplacian spectrum + 2-D spectral embedding scatter
"""
