"""Dataset adapters.

Planned contents:
  * ``toy.py``        — synthetic blobs / planted-partition graphs for unit demos.
  * ``cityscapes.py`` — Cityscapes driving dataset (19-class eval), the primary
                        benchmark. Images live under ``datasets/`` (gitignored);
                        this module only wraps loading + label remapping.
"""
